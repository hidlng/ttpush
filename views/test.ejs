<!DOCTYPE html>
<html>
<head>
    <title>이미지 버튼 예제</title>
    <style>
        .image-button {
            cursor: pointer; /* 마우스 커서를 손가락 모양으로 변경 */
            border: none; /* 테두리 없애기 */
            outline: none; /* 클릭 시 나타나는 외곽선 제거 */
            transition: transform 0.3s; /* 클릭 효과를 위한 전환 */
        }

        .image-button:active {
            transform: scale(0.98); /* 버튼 클릭 시 약간 축소되는 효과 */
        }
    </style>
</head>
<body>

<img src="/images/logo7.png" alt="버튼 이미지" id="imageButton" class="image-button">

<script>
    document.getElementById("imageButton").addEventListener("click", function() {
        alert("이미지 버튼 클릭됨!");
        // 버튼 클릭 시 실행할 기능 추가
    });
</script>

</body>
</html>
